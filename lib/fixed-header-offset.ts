/** Top padding so content clears the fixed header (stacked logo + nav pill on mobile). */
export const FIXED_HEADER_OFFSET_CLASS =
  "pt-[max(10.25rem,env(safe-area-inset-top)+8.25rem)] sm:pt-[max(10.5rem,env(safe-area-inset-top)+8.5rem)] lg:pt-28" as const;
